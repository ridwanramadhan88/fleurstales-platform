# Fleurstales OS — Independent Build

Internal staff and owner application. This build has no public storefront route or storefront entry button.

It remains compatible with the same persistence/backend contracts used by the storefront build.

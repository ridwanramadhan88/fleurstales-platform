# Fleurstales Storefront — Independent Build

Standalone public customer storefront. This build opens directly to the store and has no staff login or OS navigation.

It remains compatible with the same persistence/backend contracts used by the OS build. For real cross-device operation, both deployments must point to the same configured backend.
